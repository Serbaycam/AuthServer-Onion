@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // true = Force Load (Sayfayı tam yenileyerek git, cookie tazelensin)
        Navigation.NavigateTo("/login", true);
    }
}