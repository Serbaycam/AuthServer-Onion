# Generate independent random values; do not commit .env.
POSTGRES_PASSWORD=
JWT_SECRET=
# Enable for a controlled bootstrap/migration run; disable afterwards.
DATABASE_INITIALIZE=false
BOOTSTRAP_ADMIN_EMAIL=
BOOTSTRAP_ADMIN_PASSWORD=
